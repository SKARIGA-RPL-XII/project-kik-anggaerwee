import type * as ort from "onnxruntime-node";
import type { Box, RecognitionOptions, RecognitionServiceOptions } from "../interface";
import { Image } from "../utils/image";
export interface RecognitionResult {
    text: string;
    box: Box;
    confidence: number;
}
export interface SingleRecognitionTask {
    index: number;
    image: Image;
    box: Box;
    charWhiteSet?: Set<string>;
}
export declare class RecognitionService {
    private readonly options;
    private readonly session;
    private readonly ortModule;
    constructor(ortModule: typeof ort, session: ort.InferenceSession, options?: Partial<RecognitionServiceOptions>);
    run(image: Image, detection: Box[], options?: RecognitionOptions): Promise<RecognitionResult[]>;
    private processBox;
    private sortResultsByReadingOrder;
    private runInference;
    private ctcLabelDecode;
}
